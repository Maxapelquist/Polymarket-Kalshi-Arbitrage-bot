//! Matching-moduler för event- och contract-matching

pub mod event_matcher;
pub mod contract_matcher;

// Exports are used directly in main.rs, not here
// pub use event_matcher::{EventMatcher, EmbeddingOnlyMatcher, KalshiEvent, PolymarketEvent, EventCandidate, KalshiEventCandidates, PolymarketCandidate};
// pub use contract_matcher::ContractMatcher;
